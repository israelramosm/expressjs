# Note we depend on NODE_ENV being set to dictate which of the env variables below get loaded at runtime. 

# express port
PORT=4000

# APIs
BASE_API=/api
v1_API=/api/v1
v2_API=/api/v2

# DB URI connection
MONGODB_URI_LOCAL=mongodb://sheloapp:pwdsheloapp@localhost:27017/shelodb
MONGODB_URI_TEST=

SALT_WORK_FACTOR=10;
SESSION_SECRET=sessionsecret
